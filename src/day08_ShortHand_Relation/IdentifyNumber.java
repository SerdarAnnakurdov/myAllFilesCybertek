package day08_ShortHand_Relation;

public class IdentifyNumber {
    public static void main(String[] args) {
        int number = -29;

        boolean isPossitive = number > 0; //if number is greater than zero its possitive
        boolean isNegative = number < 0;// if number less than zero its negative
        boolean isZero = number == 0;

        System.out.println(number+" is possitive number : "+isPossitive);
        System.out.println(number+" is negative number :"+isNegative);
        System.out.println(number+" is zero  "+isZero);

        int angle1= 30;
        int angle2= 30;
        int angle = 90;
        // sum = 150 ;
        // output is : The triangle is valid : false





















    }
}
